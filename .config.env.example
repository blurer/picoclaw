# Ollama Configuration
# Copy this file to .config.env and adjust as needed

# Ollama API endpoint (no API key required)
OLLAMA_API_BASE=http://localhost:11434

# Provider and model settings
PICOCLAW_AGENTS_DEFAULTS_PROVIDER=ollama
PICOCLAW_AGENTS_DEFAULTS_MODEL=llama3.2

# Optional: Remote Ollama instance (e.g., on another machine)
# OLLAMA_API_BASE=http://192.168.1.100:11434

# Optional: API key (only if your Ollama instance requires auth)
# OLLAMA_API_KEY=
